{
  "name": "pow-coach",
  "private": true,
  "type": "module",
  "description": "PoW Coach — Proof of Workout (statique + Cloudflare Pages Functions, rien à builder)",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
